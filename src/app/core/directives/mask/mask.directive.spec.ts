import { ScMaskDirective } from './mask.directive';

describe('MaskDirective', () => {
  it('should create an instance', () => {
   // const directive = new ScMaskDirective();
   // expect(directive).toBeTruthy();
  });
});
